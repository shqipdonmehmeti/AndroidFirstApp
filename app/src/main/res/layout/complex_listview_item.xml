<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/ivSingerPhoto"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:src="@drawable/ic_launcher_background"
        />

    <TextView
        android:id="@+id/tvSingerFirstName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="25sp"
        android:textColor="@color/Black"
        android:layout_toRightOf="@id/ivSingerPhoto"
        android:text="Test First Name"
        android:layout_marginLeft="15dp"
        />

    <TextView
        android:id="@+id/tvSingerLastName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="25sp"
        android:textColor="@color/Black"
        android:layout_toRightOf="@id/ivSingerPhoto"
        android:layout_below="@id/tvSingerFirstName"
        android:text="Test Last Name"
        android:layout_marginTop="10dp"
        android:layout_marginLeft="15dp"
        />

    <TextView
        android:id="@+id/tvSingerAge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="25sp"
        android:textColor="@color/Black"
        android:layout_toRightOf="@id/ivSingerPhoto"
        android:layout_below="@id/tvSingerLastName"
        android:text="Test Age"
        android:layout_marginTop="10dp"
        android:layout_marginLeft="15dp"
        />

</RelativeLayout>